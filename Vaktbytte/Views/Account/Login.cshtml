@model Vaktbytte.Models.LoginViewModel

@{
    ViewBag.Title = "Log in";
}

<hgroup class="title">
    <h1>@ViewBag.Title.</h1>
</hgroup>
<div class="row-fluid">
    <div class="span6">
        <section id="loginForm">
            @using (Html.BeginForm(new { ReturnUrl = ViewBag.ReturnUrl }))
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary(true)
                <fieldset class="form-horizontal">
                    <legend>Logg inn</legend>
                    <div class="control-group">
                        @*@Html.LabelFor(m => m.UserName, new { @class = "control-label" })*@
                        Ditt telefonnummer (6 siffer)
                        <div class="controls">
                            @Html.TextBoxFor(m => m.UserName, new { placeholder="8 siffer"})
                            @Html.ValidationMessageFor(m => m.UserName, null, new { @class = "help-inline" })
                        </div>
                    </div>
                    <div class="control-group">
                        Hemmelig kode
                        @*@Html.LabelFor(m => m.Password, new { @class = "control-label" })*@
                        <div class="controls">
                            @Html.PasswordFor(m => m.Password)
                            @Html.ValidationMessageFor(m => m.Password, null, new { @class = "help-inline" })
                        </div>
                    </div>
                    <div class="control-group">
                        <div class="controls">
                            <label class="checkbox">
                                @Html.CheckBoxFor(m => m.RememberMe, new { @checked="checked"})
                                @Html.LabelFor(m => m.RememberMe)
                            </label>
                        </div>
                    </div>
                    <div class="form-actions no-color">
                        <input type="submit" value="Log in" class="btn" />
                    </div>
                </fieldset>
                <p>
                    @*@Html.ActionLink("Register", "Register") if you don't have a local account.*@
                </p>
            }
        </section>
    </div>
    <div class="span6">
        <section id="socialLoginForm" style="display:none;">

            @Html.Action("ExternalLoginsList", new { ReturnUrl = ViewBag.ReturnUrl })
        </section>
    </div>
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}